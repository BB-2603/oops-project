import React, { Component } from "react";

export class Intro extends Component {
    render() {
        return (
            <div>
                This is the intro page. This will contain all the info of website and
                the team members.
            </div>
        );
    }
}

export default Intro;
